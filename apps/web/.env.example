# Copiar para .env. Não é obrigatório em DEV porque o proxy do Vite trata da ligação
# ao backend (ver vite.config.js). Adicionar aqui variáveis que comecem por VITE_
# para que fiquem disponíveis no código FE via import.meta.env.
